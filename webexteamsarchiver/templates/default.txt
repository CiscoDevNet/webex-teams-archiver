Title: {{ room.title }}
Created by {{ room_creator.displayName }} ({{ room_creator.emails[0] }}) on {{ room.created|datetime_format(timestamp_format) }} and last had activity on {{ room.lastActivity|datetime_format(timestamp_format) }}.

{% for msg in messages %}
    {{- msg.created|datetime_format(timestamp_format) }} {{ msg.personEmail }}: {{ msg.text }}
    {% if msg.files %}
        {% for url in msg.files %}
            {{- msg.created|datetime_format(timestamp_format) }} {{ msg.personEmail }}: Attachment: {{ files[url].filename }} ({{ files[url].content_length }} bytes, {{ files[url].content_type }})
        {% endfor %}
    {% endif %}
{% endfor %}